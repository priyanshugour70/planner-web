/**
 * Planner is the product (and public-facing company name in chrome).
 * Feature areas (Goals, Finance, Notes, …) are **modules** — see `WORKSPACE_MODULES` in `@/lib/nav/modules`.
 */
export const PRODUCT_NAME = "Planner";
